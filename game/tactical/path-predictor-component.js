import { default as Component} from '@core/component'

export default class PathPredictorComponent extends Component {
    constructor(payload) {
        super();
        this.componentType = "PathPredictorComponent"
    }
}